package com.example.mongo_security.security.utils

object SecurityConstant {
    object Authentication {
        const val ENDPOINT = "/security/sign-in"
    }

    object ClaimKey {
        const val AUTHORITY = "authority"
    }
}
